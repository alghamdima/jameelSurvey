# تقرير تقدم المشروع وتتبّع المراحل (Progress Tracker)

## نظرة عامة
- تاريخ البدء: 2026-09-13
- حالة المشروع: تنفيذ المرحلة 1 (الأساس والتصميم ودعم اللغتين)

---

## سجل المراحل

### [x] تصحيح تجربة الاستخدام والمساران المنفصلان (Completed & Verified)
- **الحالة:** مكتملة وتم التحقق منها بالكامل (Fully Verified)
- **المُنجز الفعلي:**
  1. **مسار الأدمن:**
     - تحويل الرابط الرئيسي `/` مباشرة إلى `/admin/login` لغير المسجلين، أو إلى `/admin/dashboard` إذا كان مسجلاً بالفعل.
     - تنظيف واجهة الدخول لتقتصر حصراً على الشعار واسم المنصة واسم المستخدم وكلمة المرور وزر الدخول ومبدل اللغة.
     - تشفير كلمات المرور باستخدام PBKDF2-HMAC-SHA256 وتوثيق الجلسات بكوكيز آمنة وموقعة.
     - حماية كامل لوحة الإدارة ومسارات التعديل والنتائج بمنع الوصول غير المصرح به وإعادة التوجيه إلى تسجيل الدخول.
     - بناء لوحة تحكم إدارية كاملة تتيح استعراض الاستبيانات، وحالاتها (مسودة، منشور، مغلق)، وإحصائيات المشاركات.
     - توفير روابط المشاركة `/s/{public_id}` مبنية على `BASE_URL` مع زر نسخ سريع إلى الحافظة.
     - توحيد إجراء الإنشاء في زر واحد واضح: **«إنشاء استبيان»** في الهيدر واللوحة وإلغاء خيارات الذكاء الاصطناعي.
  2. **مسار الموظف (Creative UI & Theming):**
     - تصميم استبيان تفاعلي عصري (Creative HTML/CSS UI) بنظام البطاقات والأزرار التفاعلية الكبيرة (Interactive Button Tiles) مع شريط تقدم حي متجاوب (Live Progress Bar) يحسب نسبة إنجاز الأسئلة لحظياً، وتأثيرات بصرية بهوية عبد اللطيف جميل للتمويل.
     - دعم تخصيص صورة الغلاف العلوية (Header Banner) وخلفية الصفحة (Background Image/Color)، مع إمكانية الرفع المباشر للملفات وحفظها في `app/static/uploads` أو إدخال روابط خارجية.
     - إمكانية الاختيار بين نمطين للمظهر: إبداعي حديث (Creative) أو كلاسيكي تقليدي (Classic).
     - اعتماد أمثلة واقعية ومؤسسية متعارف عليها (مثل استبيانات الرضا الوظيفي، وتجربة الموظفين، وتقييم التدريب والمبادرات).
     - فتح رابط الاستبيان `/s/{public_id}` مباشرة في نافذة الموظف دون أي طلب لدخول أو حساب أو كلمة مرور.
     - ظهور Modal إجباري يطلب الرقم الوظيفي للبدء ويمنع التجاوز بالنقر بالخارج أو زر Escape.
     - معالجة الرقم الوظيفي كنص للحفاظ على الأصفار المبتدئة، وتوحيد الأرقام المشرقية (٠-٩) إلى قياسية، وإزالة المسافات.
     - فحص المشاركة السابقة؛ إذا كان الموظف قد شارك مسبقاً، يُمنع فوراً برسالة واضحة، ولا تفتح الأسئلة.
     - عند إدخال رقم جديد، يُنشأ توكن مشفر وموقع رقمياً (HMAC-SHA256) كـ Participation Context، ثم تفتح الأسئلة بسلاسة دون استهلاك حق المشاركة.
     - عند الإرسال، يُعاد التحقق ويتم الحفظ داخل Transaction مع قيد `UNIQUE(survey_id, employee_id)` في قاعدة البيانات.
     - اختبار منع التكرار المتزامن بـ 10 طلبات متزامنة في نفس اللحظة (نجحت مشاركة واحدة فقط وصُدت الـ 9 الأخرى).
     - اختبار مشاركة نفس الرقم الوظيفي في استبيان آخر مستقل ونجاحها تماماً.
     - عرض صفحة إغلاق واضحة للاستبيانات المغلقة دون طلب الرقم الوظيفي، وحجب الاستبيانات المسودة (404).
  3. **تطبيق الهوية والشعار الرسمي للشركة:**
     - استبدال المربع الأصفر والنصوص القديمة بالشعار الرسمي المعتمد لشركة عبد اللطيف جميل للتمويل (PNG بدقة عالية وخلفية شفافة من مجلد `images/`).
     - إزالة نصوص "منصة الاستبيانات والتصويتات" من الهيدر وشاشات الدخول والنافذة المنبثقة، والاكتفاء بالشعار المؤسسي الرسمي فقط.
     - تصحيح التنسيق وضمان توافق تام مع شاشات الجوال والحواسب باللغتين العربية والإنجليزية.

---

## اختبارات القبول المنفذة فعلياً:
- [x] فتح `/` دون جلسة أدمن يعيد التوجيه برمز 303 إلى `/admin/login`.
- [x] فتح رابط استبيان منشور `/s/abc12345` يعرض نافذة الرقم الوظيفي دون طلب تسجيل دخول وبمعزل عن لوحة الإدارة.
- [x] إدخال رقم جديد يفتح الأسئلة بسلاسة ويظهر بيانات الموظف.
- [x] إدخال الرقم فقط دون إرسال الإجابات لا يستهلك حق المشاركة (عدد المشاركات ظل 0).
- [x] إرسال الإجابات يسجل مشاركة واحدة بنجاح ويعرض صفحة الشكر.
- [x] إعادة إدخال نفس الرقم يمنع فتح الأسئلة ويعرض «سبق أن شاركت في هذا الاستبيان».
- [x] الإرسال المتزامن عبر 10 مسارات متزامنة نجح فيه طلب واحد وسُجلت محاولات التكرار كمنع فوري دون تجاوز الـ UNIQUE constraint.
- [x] نفس الرقم الوظيفي (004812) استطاع المشاركة في استبيان ثانٍ مستقل بنجاح تام.
- [x] محاولات الوصول المباشر لمسارات الإرسال وتعديل الاستبيانات صُدّت برمز 401 لغير الموثقين و303 لغير الأدمن.
- [x] التحقق البصري عبر المتصفح باللغتين العربية والإنجليزية وتأكيد تناسق الهوية وسهولة الاستخدام.

---

### [ ] المرحلة 2: لوحة الإدارة وإنشاء الاستبيانات يدويًا
- **الحالة:** لم تبدأ بعد (Pending Phase 1 Sign-off)

---

### [ ] المرحلة 3: مشاركة الموظفين ومنع التكرار
- **الحالة:** لم تبدأ بعد

---

### [ ] المرحلة 4: إنشاء الاستبيانات بمساعدة LLM
- **الحالة:** لم تبدأ بعد

---

### [ ] المرحلة 5: النتائج والتصدير
- **الحالة:** لم تبدأ بعد

---

### [ ] المرحلة 6: التحقق والتسليم
- **الحالة:** لم تبدأ بعد

---

## كيفية التشغيل السريع
### محلياً (Local Development):
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
alembic upgrade head
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### عبر Docker Compose:
```bash
docker compose up --build -d
```
