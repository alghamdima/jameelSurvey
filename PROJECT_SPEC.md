# مواصفات المشروع: منصة الاستبيانات والتصويتات الداخلية للموظفين
# Employee Surveys & Polls Platform (Jameel Surveys)

## 1. نظرة عامة والهدف (Overview & Objective)
منصة ويب داخلية مخصصة لموظفي شركة **عبد اللطيف جميل للتمويل (Abdul Latif Jameel Finance)** لإنشاء وإدارة الاستبيانات والتصويتات ومشاركتها عبر روابط مستقلة، مع إمكانية الإنشاء اليدوي أو بمساعدة الذكاء الاصطناعي (LLM)، وضمان تصويت كل موظف مرة واحدة فقط لكل استبيان برقم وظيفي موحد.

## 2. الهوية البصرية ونظام التصميم (Design System & Branding)
التزام كامل بالهوية المؤسسية المعتمدة لشركة عبد اللطيف جميل للتمويل:
- **البنفسجي الإمبراطوري الأساسي (Primary Purple):** `#3C1053` (درجة الهيدر والأساسيات: `#3C1A56`)
- **البنفسجي العميق (Deep Purple):** `#32134C`
- **البنفسجي الفاتح/المتوسط (Violet):** `#AC4FC6` / `#8C5BA6`
- **الذهبي التمييزي (Gold Accent):** `#FFCD00` / `#F2CB28` (للأزرار الثانوية، الإبراز، والتحديد)
- **الرمادي المؤسسي (Corporate Gray):** `#5C6670` / `#63656A`
- **الخلفيات والبطاقات:** الأبيض الصافي `#FFFFFF`، السطح الهادئ `#F8F9FA`، والبطاقات الفاتحة `#F4EEF7`
- **أنماط الاستبيانات المتوفرة (Themes & Styles):**
  1. **النمط الإبداعي الحديث (Creative UI):** مصمم كصفحة تفاعلية متكاملة بنظام الأزرار والبطاقات الشبكية التفاعلية (Grid Tiles)، مع شريط تقدم حي متفاعل بنسبة مئوية ولحظية، وإمكانية تخصيص صورة بانر علوية وصورة أو تدرج خلفية كامل للصفحة لاستطلاعات الرأي والفعاليات وورش العمل وتصويتات الفرق والمبادرات.
  2. **النمط الكلاسيكي (Classic Standard):** استبيان تقليدي هادئ مباشر للمسوحات الرسمية والتقييمات الدورية.
- **الطباعة والخطوط:** خطوط النظام المباشرة الافتراضية عالية الأداء (`"Segoe UI", Tahoma, Arial, "Noto Naskh Arabic", sans-serif`).
- **دعم اللغتين ثنائي الاتجاه (Bi-directional):**
  - العربية: كاملة مع توجيه `dir="rtl"` ولغة `lang="ar"`.
  - الإنجليزية: كاملة مع توجيه `dir="ltr"` ولغة `lang="en"`.
  - حفظ التفضيل عبر Cookie مستمرة، وتوافق كامل عبر الجوال والكمبيوتر.

## 3. المعمارية التقنية الثابتة (Architecture Stack)
- **إطار العمل الخلفي:** FastAPI
- **محرك القوالب:** Jinja2 لعرض خوادم HTML نظيفة دون أطر عمل ثقيلة مثل React.
- **قاعدة البيانات:** SQLite3 مع تفعيل وضع WAL و Foreign Keys.
- **إدارة الترحيلات (Migrations):** Alembic.
- **خادم التطبيق:** Uvicorn.
- **الحاويات:** Dockerfile متعدد المراحل + Docker Compose مع Persistent Volume (`jameel_surveys_data`) مثبت على `/app/data`.
- **التكامل مع الذكاء الاصطناعي:** تكامل خادمي فقط (Server-side LLM Integration) بمخرجات JSON منظمة ومحققة بـ Pydantic.

## 4. المساران المنفصلان تماماً لتجربة الاستخدام (Strictly Separated User Flows)

### أ. مسار الأدمن (Admin Flow):
- فتح الرابط الأساسي `/` يُعيد التوجيه فوراً (HTTP 303) إلى `/admin/login`.
- إذا كان الأدمن مسجلاً مسبقاً ولديه كوكي مصادقة صالحة `admin_session`، يتم تحويله مباشرة إلى لوحة الإدارة `/admin/dashboard`.
- صفحة تسجيل الدخول خالية من أي روابط مشتتة أو أقسام تعريفية، وتحتوي حصراً على: الشعار المؤسسي، اسم المنصة، حقول اسم المستخدم وكلمة المرور، زر الدخول، وزر تبديل اللغة.
- لوحة الإدارة وواجهاتها محمية بالكامل بمصادقة الخادم عبر `require_admin`.
- إمكانية إنشاء الاستبيانات عبر زر موحد «إنشاء استبيان»، وتعديلها ومعاينتها ونشرها وإغلاقها والاطلاع على نتائجها المجمعة.
- لكل استبيان رابط مشاركة مستقل بالشكل `/s/{public_id}` مبني على العنوان الأساسي القابل للتهيئة `BASE_URL`، مع زر نسخ الرابط مباشرة.
- الموظفون لا يمكنهم الاطلاع على لوحة الإدارة أو قوائم الاستبيانات الأخرى.

### ب. مسار الموظف (Employee Flow):
- عند فتح رابط الاستبيان المنشور `/s/{public_id}`: لا يُطلب تسجيل دخول أو كلمة مرور أو إنشاء حساب، ولا يُحوَّل الموظف إلى صفحة الأدمن.
- تظهر فوراً نافذة منبثقة (Modal) إجبارية تطلب الرقم الوظيفي للبدء، ولا يمكن تجاوزها بالنقر بالخارج أو زر Escape.
- التحقق في الخادم من الرقم الوظيفي ومعالجته كنص للحفاظ على الأصفار المبتدئة، مع توحيد الأرقام المشرقية والعربية وإزالة المسافات.
- إذا سبق للرقم المشاركة في نفس الاستبيان، يُعرض تنبيه «سبق أن شاركت في هذا الاستبيان» وتظل الأسئلة مغلقة.
- إذا لم يشارك، يُنشئ الخادم رمز سياق مشاركة مشفر وموقع رقمياً (HMAC Token)، وتُفتح الأسئلة.
- مجرد إدخال الرقم أو فتح الأسئلة لا يُعتبر مشاركة ولا يستهلك حق الموظف.
- عند إرسال الإجابات، يتم استخراج الرقم من التوكن الموثق وحفظ الإجابات داخل Transaction واحدة مع قيد `UNIQUE(survey_id, employee_id)` لمنع التكرار حتى مع الطلبات المتزامنة.
- يمكن للموظف بنفس الرقم المشاركة في استبيانات أخرى مستقلة بحرية.
- الاستبيانات المغلقة تعرض رسالة الإغلاق مباشرة دون طلب الرقم الوظيفي، والمسودات محجوبة تماماً عن العامة برمز 404.
